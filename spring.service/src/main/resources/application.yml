eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8761/eureka/
server:
  port: 8880

spring:
  cloud:
    config:
      server:
        git:
          uri: https://github.com/Umikelgy/Springs_study.git
          searchPaths: WebManven_M/myproperties/
  application:
    name: config-server